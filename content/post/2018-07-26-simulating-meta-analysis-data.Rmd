---
title: simulating meta-analysis data
author: Charles T. Gray
date: '2018-07-26'
draft: true
slug: simulating-meta-analysis-data
categories:
  - statistics
tags:
  - 3c^2
  - simulations
---


```{r packages, message=FALSE}
# packages

library(tidyverse)

```


## meta-parameters

I want to (re)run trials of simulations for:

- different sample sizes
- different distributions
- (also different arm adjustment in the meta-analysis simulation)

```{r}
meta_parameters <- list(
  sample_size = c(15, 30),
  distribution = list(
    list(
      dist = "exp",
      pars = list(rate = 2)
    ),
    list(
      dist = "lnorm",
      pars = list(meanlog = 2, sdlog = 1)
    )
  )
) %>% purrr::cross()

# take a look
meta_parameters[1:2] %>% str()

```


```{r}
to_dist_fn <- function(rdist, type = "d") {
  
}   
  
  
```



## bind the distributions to a list

```{r}



```


## single-study simulation

### a trial

> go back to 


## meta-analysis simulation