baseurl = "/" 
title = "measured." 
theme = "hugo-creative-portfolio-theme"
languageCode = "en-us"
math = true
# Enable comments by entering your Disqus shortname
disqusShortname = "cantabile-rbind"
# Enable Google Analytics by entering your tracking code
googleAnalytics = ""

# Some other stuff I saw in the configuration selection of the bookdown book.
enableEmoji = true
ignoreFiles = ["\\.Rmd$", "\\.Rmarkdown$", "_files$", "_cache$"]


[permalinks]
    post = "/:year/:month/:day/:slug/"

[params]
  # Style options: default (pink), blue, green, pink, red, sea, violet
  # Use custom.css for your custom styling
  style = "default"
  # style = "http://style.blue.css"
  description = "Personal website of Charles T. Gray"
  author = "Charles T. Gray"
  dateFormat = "2018-05-31"
  copyright = "&copy;2018 Charles T. Gray"
  sidebarAbout = [
    "Data detective in training. My doctorate meanders somewhere between statistics and data science.",
    "Reformed musician, aspiring statistician. Coding with a view in the Australian bush, with kangaroos for company."
  ]
  # Contact page
     # Since this template is static, the contact form uses www.formspree.io as a
     # proxy. The form makes a POST request to their servers to send the actual
     # email. Visitors can send up to a 1000 emails each month for free.
     #
     # What you need to do for the setup?
     #
     # - set your email address under 'email' below
     # - upload the generated site to your server
     # - send a dummy email yourself to confirm your account
     # - click the confirm link in the email from www.formspree.io
     # - you're done. Happy mailing!
  # email = "you@yoursite.com"

  # Nav links in the side bar
  [[params.navlinks]]
  name = "home"
  url = "post/"
  home = true

  [[params.navlinks]]
  name = "about"
  url = "about/about"
  
  [[params.navlinks]]
  name = "blog"
  url = "post/"
  
  [[params.navlinks]]
  name = "projects"
  url = "projects/"

  # [[params.navlinks]]
  # name = "Get in touch"
  # url = "contact/"

  [params.social]
  # facebook      = "full page or profile url in facebook"
  # googleplus    = "full profile url in googleplus"
  email         = "charlestigray@gmail.com"
  twitter       = "https://twitter.com/cantabile"
  # linkedin      = "full profile url in linkedin"
  # stackoverflow = "full profile url in stackoverflow"
  # instagram     = "full profile url in instagram"
  github        = "https://github.com/softloud"


